<div class="card">
  <div class="card-header d-flex align-items-center justify-content-between">
    Charges

    <a data-toggle="collapse" href="#treatment_charges_new_form">
      Add charge
    </a>
  </div>

  <ul class="list-group list-group-flush">
    <% @treatment.charges.each do |charge| %>
      <li class="list-group-item d-flex align-items-center justify-content-between">
        <a href="/charges/<%= charge.id %>">
          <%= charge.amount %>
        </a>

        <a href="/delete_charge_from_treatment/<%= charge.id %>">
          Delete
        </a>

      </li>
    <% end %>

    <div class="card-footer">
      <div class="collapse" id="treatment_charges_new_form">
        <%= render partial: "treatment_templates/treatment_charges_new_form" %>
      </div>
    </div>

  </ul>
</div>
